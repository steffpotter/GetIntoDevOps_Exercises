# create and assign name variables
first_name = "Steff"
last_name = "Potter"

# add names to a list
names_list = (first_name, last_name)
print(names_list)
print(names_list[0])

# add names to a dictionary
names_dictionary = {"first": first_name, "last": last_name}
print(names_dictionary)
print(names_dictionary["first"])
